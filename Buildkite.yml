steps:
# False Positive: Include clown emoji in the clown_detect.txt file
  - label: ":clown: DetectClowns"
    plugins:
      - detect-clowns#v2.0.0: ~


#False positive: Let the bash file have an error.
  - label: ":shell: Shellcheck"
    plugins:
      - shellcheck#v1.3.0:
          files: scripts/*.sh
      
